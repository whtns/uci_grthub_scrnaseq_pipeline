name: cellbender
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.7
  - pip
  - pip:
    - torch
    - git+https://github.com/broadinstitute/CellBender.git#egg=CellBender